# DevMulti
该项目的核心逻辑是一个多 Agent 协作工作流：首先由代码扫描 Agent 读取 Git 仓库结构、依赖文件、提交 diff 和关键模块；然后由架构理解 Agent 对项目进行长链推理，判断代码所属业务逻辑、调用链路和潜在影响范围；接着由问题诊断 Agent 识别可能存在的 Bug、冗余逻辑、性能问题、类型错误和安全风险；最后由修复 Agent 生成可执行补丁，并调用测试 Agent 自动运行单元测试、Lint、类型检查或构建命令。如果测试失败，系统会把错误日志重新送回模型，进行多轮反思和二次修复，直到生成稳定版本或输出人工介入建议。
